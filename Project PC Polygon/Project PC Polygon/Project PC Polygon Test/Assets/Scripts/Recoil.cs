using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Recoil : MonoBehaviour {

    public void camRecoil(float vRecoil, float hRecoil) {

        //transform.Rotate(-vRecoil,Random.Range(-hRecoil, hRecoil),0);

        //Mathf.Clamp(this.transform.rotation.x, -90.0f, 90.0f);


    }

    void Update() {
        //Mathf.Clamp(this.transform.rotation.x, -90.0f, 90.0f);
    }
}
